{
  "name": "truckin-all-day",
  "version": "2.0.0",
  "private": true,
  "description": "Truckin all Day — Official Music Label Platform & Interactive Artist Arcade",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm --prefix android-app install && vite build && npm --prefix android-app run build && node -e \"const fs=require('fs'); fs.cpSync('android-app/dist', 'dist/app', {recursive: true});\"",
    "preview": "vite preview",
    "dev:android": "npm --prefix android-app run dev",
    "build:android": "npm --prefix android-app run build",
    "cap:sync": "npm run build:android && npm --prefix android-app run cap:sync",
    "cap:open": "npm --prefix android-app run cap:open"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
